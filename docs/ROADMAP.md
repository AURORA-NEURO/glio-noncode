# Build sequence

## Foundation delivered

- typed case, context, variation, evidence, hypothesis, validation, review, and dossier objects;
- deterministic baseline scoring with source-group dependence correction;
- event-sourced local persistence and content-addressed replay checks;
- a dependency-free CLI and loopback HTTP API;
- first-class structural-event paths, matched cohort controls, causal sensitivity, reclassification, workflow compilation, data controls, monitoring, and quality metrics;
- synthetic fixture coverage and an Actions quality matrix.

## Next implementation wave

1. Add a versioned reference manifest and adapter conformance tests. **Implemented:** addressed source receipts, access/license/context declarations, deterministic query/export projections, portable static-adapter probes, API/CLI surfaces, and fail-closed conformance states.
2. Add streaming VCF/BCF and bounded breakend import with explicit normalization reports. **Implemented:** line- and block-streamed source traversal, complete-source and header hashes, bounded row/issue retention, multiallelic and genotype policy controls, raw/BGZF BCF framing, deterministic row receipts, and explicit breakend mate-coordinate outcomes are available through the CLI and API. See [docs/STREAMING_VARIANT_IMPORT.md](STREAMING_VARIANT_IMPORT.md).
3. Add columnar interval indexes and context-lattice queries for public reference tracks. **Implemented:** bounded JSON/JSONL/CSV/TSV normalization, immutable parallel columns, block-level overlap pruning, deterministic row/index/query addresses, exact and assembly-safe lattice context matching, CLI/API surfaces, and public-boundary audit coverage. See [docs/REFERENCE_INTERVAL_INDEX.md](REFERENCE_INTERVAL_INDEX.md).
4. Replace fixture-only feature readings with adapters whose licenses and access modes are declared. **Implemented:** declared source metadata, license/access/coordinate/context contracts, columnar index-backed reads, unavailable and out-of-domain abstention states, deterministic conformance probes, manifest emission, CLI/API surfaces, public-boundary checks, and atlas integration. See [docs/REFERENCE_TRACK_ADAPTERS.md](REFERENCE_TRACK_ADAPTERS.md).
5. Add cohort split, leakage, calibration, selective-risk, and transport benchmarks. **Implemented:** deterministic aggregate-only cohort records, group/source/context/hash/temporal splits, cross-split leakage audits, held-out calibration and selective-risk reports, source-to-target transport comparisons, CLI/API/schema/capability surfaces, public-boundary checks, and regression coverage. See [docs/COHORT_BENCHMARKS.md](COHORT_BENCHMARKS.md).
6. Add a review workspace that renders evidence edges, alternatives, deltas, and provenance without reducing them to one score. **Implemented:** replay-gated provenance-first review read model, payload-free evidence views, typed edges, explicit alternatives, source-centric lineage, explainable review queue, cross-run per-dimension deltas, CLI/API/schema/capability surfaces, public-boundary checks, and regression coverage. See [docs/REVIEW_WORKSPACE.md](REVIEW_WORKSPACE.md).
7. Add authenticated deployment profiles and audit export for institutional operation. **Implemented:** explicit non-loopback policy, API-key scopes, rate limiting, redacted hash-chained request audit, profile schema, CLI handling, durable replay verification, retention controls, and API endpoints.
8. Add a repository-wide static module inventory for deep module-by-module review. **Implemented:** AST-only source discovery, line and symbol counts, local dependency graph, cycle and unresolved-edge visibility, bounded indexes and queries, explainable depth percentage, review queue, timestamp-free observability, public-boundary checks, and a ten-artifact exact-byte offline packet. See [docs/MODULE_INVENTORY.md](MODULE_INVENTORY.md).
9. Add deterministic module-change impact analysis for baseline-to-candidate review. **Implemented:** immutable module and dependency diffs, reverse-dependency propagation across both snapshots, explainable severity and risk, verification-task planning, configurable policy gates, independent closure audits, timestamp-free observability, public API/CLI surfaces, and a ten-artifact exact-byte offline packet. See [docs/MODULE_IMPACT.md](MODULE_IMPACT.md).
10. Add per-module certification and contract coverage. **Implemented:** eight static checks per module, explicit test/documentation/export/dependency/boundary/scale evidence, scored rows, gap and remediation queues, aggregate policy gates, independent audits, timestamp-free runtime observability, matrix diffs, review routing, public API/CLI surfaces, and a ten-artifact exact-byte offline packet. See [docs/MODULE_CERTIFICATION.md](MODULE_CERTIFICATION.md).
11. Add explainable certification evidence lineage and quality readiness. **Implemented:** digest-addressed source/test/documentation/export evidence, resolved and unresolved dependency edges, bounded lineage queries and exports, check-kind and family conservation, blocker/top-gap routing, readiness classification, API/CLI/schema/capability surfaces, Actions checks, and fixture-level tamper verification. See [docs/MODULE_CERTIFICATION.md](MODULE_CERTIFICATION.md).
12. Add configurable quality-policy thresholds for CI release decisions. **Implemented:** immutable strict/default policies, evidence/check/family/blocker/all-certified/ready thresholds, per-decision addresses, bounded queries and CSV output, API/CLI/schema/capability surfaces, and focused regression coverage.
13. Add a deep module implementation workbench. **Implemented:** seven-dimension module assessments, resolved graph fan-in/fan-out, evidence-aware depth bands, delivery-risk classification, family rollups, deterministic action planning, configurable policy gates, independent conservation audits, baseline-to-candidate diffs, bounded CLI/API projections, public-surface checks, Actions contract commands, and focused regression coverage. See [docs/MODULE_WORKBENCH.md](MODULE_WORKBENCH.md).
14. Add a single static workbench runtime handoff. **Implemented:** seven ordered typed stages, artifact-address retention, accepted/blocked stage states, bounded stage queries, JSON/CSV exports, CLI/API/schema/capability surfaces, public-surface checks, and runtime regression coverage.
15. Add bounded implementation-wave selection. **Implemented:** priority and impact ranking, total and per-module caps, risk and priority filters, selected family rollups, deferred-task counts, stable portfolio addresses, CLI/API/schema/capability surfaces, public-surface checks, Actions contract commands, and regression coverage.
16. Add an evidence-gated module execution ledger. **Implemented:** prerequisite-aware task state, immutable transition events, evidence-gated completion, independent graph/evidence/conservation audits, explicit progress policies, ordered runtime handoff, execution snapshot diffs, bounded CLI/API/schema/capability surfaces, public-boundary checks, and regression coverage. See [docs/MODULE_WORKBENCH_EXECUTION.md](MODULE_WORKBENCH_EXECUTION.md).
17. Add module-level execution review routing. **Implemented:** conserved per-module progress and evidence rollups, blocker and active-evidence routing, critical-task counts, bounded next-task queues, review exports, CLI/API/schema/capability surfaces, public-boundary checks, and regression coverage.
18. Add portable exact-byte execution handoff packets. **Implemented:** thirteen-artifact packet assembly, atomic filesystem writes, exact UTF-8 byte addressing, independent manifest/byte/path/canonical/public verification, offline packet queries, replay receipts, packet diffs, release gates, seven-stage packet runtime, CLI/API/schema/capability surfaces, public-boundary checks, and deep regression coverage. See [docs/MODULE_WORKBENCH_EXECUTION_PACKET.md](MODULE_WORKBENCH_EXECUTION_PACKET.md).
19. Add deterministic archive transport and archive reconciliation. **Implemented:** fixed-metadata ZIP transport, exact-byte verification and safe unpacking, addressed chunk transfer with resume and reassembly, nine-stage archive runtime, archive-to-archive member diffs, path-free archive indexes, bounded queries, CLI/API/schema/capability surfaces, public-boundary checks, and deep tamper/regression coverage.
20. Add a durable content-addressed archive object store. **Implemented:** canonical manifest/object separation, exact-byte object deduplication, append-only registration journal, expected-head concurrency guard, atomic directory writes, fail-closed loading, multi-plane verification, replay, bounded catalog queries, store diffs, lifecycle runtime, CLI/API/schema/capability surfaces, public-boundary checks, and deep tamper/regression coverage.
21. Add immutable archive-store checkpoints and ancestry reconciliation. **Implemented:** addressed checkpoint capture, JSON rehydration, exact-match and append-only-extension proofs, journal/entry divergence detection, added/missing resource reporting, bounded reconciliation queries, exports, CLI/API/schema/capability surfaces, and regression coverage.
22. Add non-mutating archive-store recovery diagnostics. **Implemented:** safe directory and manifest inspection, canonical-byte checks, bounded object-token checks, regular-file and symlink detection, exact object-address checks, missing/extra object detection, public-boundary checks, addressed finding/report receipts, bounded queries, exports, CLI/API/schema/capability surfaces, and failure-injection coverage.
23. Add deterministic archive-store replication and promotion. **Implemented:** path-free source/target plans, append-only ancestry proofs, object reuse/copy accounting, journal conflict detection, expected-head guards, atomic apply and reload receipts, promotion holds, portable fixed-vocabulary packet manifests with exact artifact byte addresses, bounded plan/query/runtime/packet projections, CLI/API/schema/capability surfaces, public-boundary checks, and deep downloaded-data regression coverage.
24. Add packet-to-packet diffing and independent release assurance. **Implemented:** verified left/right packet loading, explicit added/removed/unchanged/changed artifact actions, boundary and content-change classification, release hold/promotable decisions, ordered diff runtime stages, bounded diff/release/runtime queries, severity-bearing assurance findings, deterministic JSON/CSV/Markdown projections, CLI/API/schema/capability surfaces, identity-free public checks, and tamper/failure regression coverage.
25. Add bounded multi-packet diff matrices for release windows. **Implemented:** repeatable path-free pair specifications, per-pair diff and release decisions, conserved state/release rollups, bounded matrix queries, deterministic JSON/CSV/Markdown projections, CLI/API/schema/capability surfaces, and regression coverage for matched and divergent windows.
26. Add policy-governed release-window handoffs. **Implemented:** bounded threshold policies, explicit matrix checks, promotable/hold/blocked state semantics, seven-stage fail-closed runtimes, independent assurance findings, bounded queries and exports, CLI/API/schema/capability surfaces, identity-free checks, and persisted-directory regression coverage.
27. Add policy sensitivity analysis for release windows. **Implemented:** analysis-only scenario comparisons over one verified matrix, strict/review threshold tradeoffs, conserved state counts, deterministic best-promotable references, bounded scenario queries and exports, CLI/API/schema/capability surfaces, identity-free checks, and persisted-directory regression coverage.
28. Add an append-only release-window decision ledger. **Implemented:** explicit promote/hold/block/supersede entries, evidence-linked chain continuity, fail-closed promote semantics, bounded ledger/runtime/assurance/diff queries and exports, deterministic revision comparison, CLI/API/schema/capability surfaces, identity-free checks, and persisted-directory regression coverage.
29. Add a durable release-window review-store transport. **Implemented:** exact three-artifact manifests, atomic writes, canonical-byte and regular-file checks, persisted ledger rehydration, replay receipts, optimistic head guards, bounded store/operation/check/entry queries, eight-stage runtime receipts, independent assurance findings, exact/append-only/divergent store diffs, CLI/API/schema/capability surfaces, identity-free checks, and deep tamper/failure regression coverage.
30. Add a durable catalog and federation layer above review stores. **Implemented:** deterministic multi-store catalogs with append-only registration journals, exact three-artifact catalog persistence, catalog hydration and bounded queries, eight-stage catalog runtime closure, window and ledger federation policy checks, accepted-but-held readiness semantics, exact/append-only/divergent catalog diffs, CLI/API/schema/capability surfaces, public-surface audit integration, Actions coverage, and real downloaded-store verification.
31. Add independent catalog assurance and a release gate. **Implemented:** recomputed catalog/linkage/journal/window/hydration/public findings, warning-versus-blocker semantics, addressed assurance queries, a combined catalog/runtime/federation/assurance gate, held-versus-blocked release decisions, deterministic exports, CLI/API/schema/capability surfaces, identity-free audit and Actions coverage, and downloaded-data verification.
32. Add a portable catalog release packet. **Implemented:** exact-byte manifest transport for catalog/runtime/federation/assurance/gate projections, atomic six-file persistence, canonical JSON and file-set enforcement, nested component rehydration, addressed transport/query receipts, held and blocked packet preservation, CLI/API/schema/capability surfaces, tamper regression coverage, and downloaded-catalog round trips.
33. Add packet transition comparison and release review decisions. **Implemented:** deterministic five-artifact packet actions, exact-versus-changed classification, release-state transition mapping, addressed diff checks and bounded queries, promote/hold/block/supersede decisions, append-only review chains with optimistic head guards, exact two-file review persistence, CLI/API/schema/capability surfaces, tamper regression coverage, and real downloaded-packet demonstrations.
34. Add independent packet-review assurance and a release gate. **Implemented:** review and diff recomputation outside the decision builder, warning-versus-blocker findings, addressed assurance receipts, promote/hold/block/supersede gate closure, held-versus-blocked state classification, exact two-file assurance and gate persistence, bounded query/export projections, CLI/API/schema/capability surfaces, public-boundary audit integration, Actions coverage, tamper regression coverage, and downloaded-packet round trips.
35. Add longitudinal packet-review gate history. **Implemented:** append-only promote/hold/block/supersede head records, optimistic expected-head protection, contiguous entry ancestry, conserved decision counters, valid held and blocked history persistence, exact two-file history archives, canonical-byte and manifest verification, bounded summary/entry/check queries, deterministic JSON/CSV/Markdown projections, CLI/API/schema/capability surfaces, public-surface audit integration, Actions coverage, and downloaded-packet verification.
36. Add deterministic replay for packet-review gate history. **Implemented:** start-to-terminal state reconstruction, gate/head linkage, transition-chain checks, terminal release projection, addressed replay reports, bounded event/check queries, deterministic JSON/CSV/Markdown exports, CLI/API/schema/capability surfaces, public-surface audit integration, Actions coverage, and real downloaded-packet demonstration coverage.
37. Add a longitudinal packet-review gate history observatory. **Implemented:** ordered observation projections over verified history archives, stable/promoted/recovered/regressed/held/blocked/superseded/changed transition classification, conserved rollups, independent checks, bounded observation/transition/check queries, exact two-file persistence, deterministic JSON/CSV/Markdown exports, CLI/API/schema/capability surfaces, public-surface audit integration, Actions coverage, and real downloaded-packet verification.
38. Add policy-governed observatory runtime closure. **Implemented:** bounded release policies, eight addressed policy checks, fail-closed load/verify/policy/project/complete stages, ready/held/blocked runtime states, exact two-file runtime persistence, bounded stage/policy-check queries, deterministic JSON/CSV/Markdown exports, CLI/API/schema/capability surfaces, public-surface audit integration, and real downloaded-packet runtime demonstration coverage.
39. Add a portable observatory closure packet. **Implemented:** exact five-file handoff for observatory, independent verification, policy, and runtime projections; acyclic content addressing; deterministic runtime replay checks; canonical-byte and nested-link validation; bounded artifact, observation, transition, stage, and policy-check queries; JSON/CSV/Markdown exports; CLI/API/schema/capability surfaces; public-surface audit integration; and real downloaded-packet round trips.
40. Add a deterministic observatory packet registry. **Implemented:** unique addressed packet entries, ready/held/blocked and acceptance conservation, independent packet-metadata linkage checks, exact four-file registry persistence, canonical-byte and manifest verification, bounded entry/packet/check queries, deterministic JSON/CSV/Markdown exports, CLI/API/schema/capability surfaces, public-surface audit integration, and multi-packet downloaded-data coverage.
41. Add observatory packet registry federation. **Implemented:** deterministic aggregation of independently verified registries, explicit registry and packet conservation, bounded federation policy checks, ready/held/blocked/empty state semantics, independent verification and runtime replay, exact six-file federation persistence, canonical-byte and manifest verification, bounded registry/packet-rollup/check/stage queries, deterministic JSON/CSV/Markdown exports, CLI/API/schema/capability surfaces, public-surface audit integration, and real downloaded-data federation coverage.
42. Add federation assurance and release gating. **Implemented:** independent federation and runtime replay, 21 addressed assurance findings, warning-versus-blocker classification, 15 conserved release-gate checks, promote/hold/block decisions, exact three-file gate persistence, canonical-byte and manifest verification, bounded finding/check queries, deterministic JSON/CSV/Markdown exports, CLI/API/schema/capability surfaces, public-surface audit integration, and real downloaded-data gate coverage.
43. Add operational federation review routing and snapshot diffs. **Implemented:** addressed queue items for every assurance finding and release-gate check, clear/review/blocked routing with high/critical priorities, exact two-file queue persistence, added/removed/changed/unchanged/resolved diff classification, bounded queue and diff queries, deterministic JSON/CSV/Markdown exports, CLI/API/schema/capability surfaces, public-surface audit integration, and real downloaded-data review coverage.
44. Add append-only federation review decision ledgers. **Implemented:** bounded acknowledge/remediate/waive/escalate/reopen actions, evidence-required remediation, critical-waiver prevention, optimistic head guards, replayed item closure counts, source-gate-authoritative release semantics, exact three-file persistence, decision snapshot diffs, bounded queries, deterministic exports, CLI/API/schema/capability surfaces, public-surface audit integration, Actions coverage, and real downloaded-data decision round trips.
45. Add independent assurance for federation review decision ledgers. **Implemented:** 12 recomputed linkage, chain, evidence, policy, readiness, count, state, and public-boundary findings; required-versus-optional severity; an 8-check promote/hold/block gate; source-queue-authoritative readiness; exact three-file assurance persistence; bounded finding/check queries; deterministic JSON/CSV/Markdown exports; CLI/API/schema/capability surfaces; public-surface audit integration; Actions coverage; tamper/failure regression coverage; and real downloaded-data assurance round trips.
46. Add independent assurance snapshot diffs. **Implemented:** stable finding/check joins across two verified assurance gates; added/removed/unchanged/changed records; improved/regressed/mixed outcome state; baseline/candidate address retention; bounded action/plane/text queries; exact two-file diff persistence; deterministic JSON/CSV/Markdown exports; CLI/API/schema/capability surfaces; public-surface audit integration; Actions contracts; tamper/failure regression coverage; and real downloaded-data comparison coverage.
47. Add longitudinal decision-assurance history. **Implemented:** append-only verified gate observations; unique snapshot IDs and optimistic head guards; initial/stable/improved/regressed/changed transitions; ready/held/blocked state retention; conserved quality and readiness counts; independent seven-check replay; exact three-file persistence; bounded state/transition/text queries; deterministic JSON/CSV/Markdown exports; CLI/API/schema/capability surfaces; public-surface audit integration; Actions contracts; tamper/failure regression coverage; and real downloaded-data history round trips.
48. Add multi-history decision-assurance series. **Implemented:** deterministic history aggregation; ready/held/blocked/mixed projections; conserved observation and transition totals; added/removed/unchanged/changed series diffs with improved/regressed directions; independent eight-check replay; exact three-file series persistence; bounded history/state queries; deterministic JSON/CSV/Markdown exports; CLI/API/schema/capability surfaces; public-surface audit integration; Actions contracts; tamper/failure regression coverage; and real downloaded-data series round trips.
49. Add series policy evaluation. **Implemented:** configurable coverage, blocked, held, acceptance, readiness, and mixed-state rules; nine addressed policy checks; passed/hold/blocked outcomes; exact three-file policy-evaluation persistence; canonical-byte and manifest verification; CLI/API/schema/capability surfaces; public-surface audit integration; and downloaded-data policy coverage.
50. Add a portable decision-assurance series release handoff. **Implemented:** eight-stage release closure over independently verified series, policy, and evaluation receipts; ready/hold/blocked semantics; exact five-file package persistence; canonical-byte and manifest verification; bounded release and diff queries; deterministic JSON/CSV/Markdown projections; release-to-release added/removed/unchanged/changed comparisons with improved/regressed direction; CLI/API/schema/capability surfaces; public-surface audit integration; Actions contracts; and downloaded-packet release demonstrations.
51. Add a release-package admission registry. **Implemented:** verified-package admission, stable package/release provenance entries, ready/hold/blocked and acceptance conservation, exact three-file registry persistence, canonical-byte and manifest verification, bounded registry and diff queries, deterministic JSON/CSV/Markdown projections, CLI/API/schema/capability surfaces, public-surface audit integration, Actions contracts, and registry transition failure coverage.
52. Add release-registry federation. **Implemented:** deterministic aggregation of independently verified release registries, source-scoped member and package identities, readiness and state conservation, bounded policy evaluation, independent structural verification, five-stage runtime closure, exact eight-file federation persistence, canonical-byte and manifest verification, bounded member/package/check/stage queries, federation diffs, deterministic JSON/CSV/Markdown projections, CLI/API/schema/capability surfaces, public-surface audit integration, Actions coverage, and persisted-directory failure coverage.
53. Add an independent release-registry federation assurance gate. **Implemented:** recomputed federation and runtime findings, blocker-versus-warning semantics, independent promote/hold/block decisions, exact three-file assurance-gate persistence, manifest byte receipts, bounded finding/check queries, deterministic JSON/CSV/Markdown projections, CLI/API/schema/capability surfaces, public-surface audit integration, Actions coverage, and downloaded-data gate demonstrations.
54. Add operational release-registry federation gate review routing. **Implemented:** deterministic queue items for every source finding and gate check, clear/review/blocked initial states, high/critical priorities, independently recomputed queue verification, exact queue persistence, bounded item/finding/check queries, append-only acknowledge/remediate/waive/escalate/reopen decisions, evidence requirements, critical-waiver prevention, optimistic head guards, replayed item closure, source-gate-authoritative readiness, decision-ledger persistence, ledger queries, ledger snapshot diffs, CLI/API/schema/capability surfaces, public-surface audit integration, Actions coverage, and downloaded-data review demonstrations. See [docs/RELEASE_REGISTRY_FEDERATION_GATE_REVIEW.md](RELEASE_REGISTRY_FEDERATION_GATE_REVIEW.md).
55. Add independent assurance for release-registry federation gate review decision ledgers. **Implemented:** fourteen independently recomputed ledger, queue, entry-chain, action-count, evidence-policy, transition, replay, source-authority, readiness, address, and public-boundary findings; ten promote/hold/block gate checks; exact three-file assurance persistence; exact two-file assurance diffs; bounded assurance and diff queries; deterministic JSON/CSV/Markdown exports; CLI/API/schema/capability surfaces; public-surface audit registration; Actions coverage; tamper/failure regression coverage; and current-format downloaded-data round trips. See [docs/RELEASE_REGISTRY_FEDERATION_GATE_REVIEW_DECISION_LEDGER_ASSURANCE.md](RELEASE_REGISTRY_FEDERATION_GATE_REVIEW_DECISION_LEDGER_ASSURANCE.md).
56. Add longitudinal assurance history for release-registry federation gate review decision ledgers. **Implemented:** append-only verified assurance-gate observations; deterministic initial/stable/improved/regressed/changed transitions; conserved readiness and quality counters; explicit ready/held/blocked terminal state retention; optimistic head guards; independent history and diff recomputation; exact three-file history and exact two-file diff packages; canonical-byte, manifest, regular-file, symlink, and legacy-shape rejection; bounded history and diff queries; deterministic JSON/CSV/Markdown projections; CLI/API/schema/capability surfaces; public-surface registration; Actions coverage; and a runnable current-format downloaded-data demonstration. See [docs/RELEASE_REGISTRY_FEDERATION_GATE_REVIEW_DECISION_LEDGER_ASSURANCE_HISTORY.md](RELEASE_REGISTRY_FEDERATION_GATE_REVIEW_DECISION_LEDGER_ASSURANCE_HISTORY.md).
57. Add a cross-run assurance-history observatory. **Implemented:** source-scoped members over independently verified history packages; deterministic empty/ready/held/blocked/mixed state folding; conserved entry, transition, gate, finding, and check totals; independent eight-check verification; exact five-file observatory persistence; derived metrics; canonical-byte and manifest receipts; regular-file, symlink, extra-file, tamper, and legacy-shape rejection; bounded member, verification-check, and diff queries; deterministic JSON/CSV/Markdown projections; member-level added/removed/unchanged/changed comparisons with improved/regressed/mixed directions; CLI/API/schema/capability surfaces; public-surface registration; Actions coverage; and current-format downloaded-data demonstration. See [docs/RELEASE_REGISTRY_FEDERATION_GATE_REVIEW_DECISION_LEDGER_ASSURANCE_HISTORY_OBSERVATORY.md](RELEASE_REGISTRY_FEDERATION_GATE_REVIEW_DECISION_LEDGER_ASSURANCE_HISTORY_OBSERVATORY.md).
58. Add a portable observatory archive and resumable transfer boundary. **Implemented:** deterministic fixed-metadata ZIP transport over the exact five-file observatory; byte-oriented archive loading and verification; canonical archive manifest and artifact receipts; exact extraction; bounded archive queries; deterministic chunk transfer manifests; contiguous byte-range and chunk-hash conservation; manifest-only inventory with missing-chunk queries; fail-closed reassembly through the nested archive verifier; exact transfer-directory persistence; CLI/API/schema/capability surfaces; public-surface registration; Actions coverage; negative controls; and current-format downloaded-archive transfer demonstration. See [docs/RELEASE_REGISTRY_FEDERATION_GATE_REVIEW_DECISION_LEDGER_ASSURANCE_HISTORY_OBSERVATORY_ARCHIVE_TRANSFER.md](RELEASE_REGISTRY_FEDERATION_GATE_REVIEW_DECISION_LEDGER_ASSURANCE_HISTORY_OBSERVATORY_ARCHIVE_TRANSFER.md).
59. Add independent archive-transfer audit and partial recovery. **Implemented:** eight addressed graph/range/boundary/manifest/receipt/progress/nested-archive/completion checks; complete versus incomplete transfer states; manifest-only and persisted partial-directory audits; fail-closed tamper and count conservation; CLI/API/schema/capability surfaces; public-surface registration; and resumed out-of-order finalization coverage.
60. Add value-free contract compatibility and release gating. **Implemented:** structural field/member/type classifications with safe/review/breaking outcomes; configurable allowed outcomes, review/breaking thresholds, resource allowlists, and source-audit requirements; independent gate, query, and runtime audits; exact seven-file persistence with nested diff/gate/query replay; CLI/API/schema/capability surfaces; public-surface registration; Actions coverage; and a real ZIP-backed compatibility demonstration that preserves blocked changes without exposing source values.
61. Add value-free compatibility remediation planning. **Implemented:** deterministic none/review/repair/migrate/restore/investigate action mapping; requiredness and priority conservation; blocked/review/clear plan dispositions; independent plan and query audits; bounded action queries; exact seven-file remediation runtime persistence; CLI/API/schema/capability surfaces; public-surface registration; and a repeatable real downloaded-ZIP demonstration.
62. Add value-free remediation resolution and closure tracking. **Implemented:** addressed pending/resolved/waived/rejected/not-applicable dispositions; required-action conservation; clear/promote, review/hold, and blocked/block state folding; independent resolution, query, and exact-runtime audits; bounded status queries; seven-file resolution-runtime persistence; CLI/API/schema/capability surfaces; public-surface registration; and a real downloaded-ZIP pending-resolution demonstration.
63. Add value-free remediation resolution history and trend tracking. **Implemented:** append-only addressed snapshots, initial/improved/regressed/unchanged transitions, latest-state folding, independent history/query/runtime audits, bounded history queries, exact six-file runtime persistence, CLI/API/schema/capability surfaces, public-surface registration, Actions coverage, and a real downloaded-ZIP history demonstration.
64. Add value-free remediation resolution history diffs. **Implemented:** baseline/candidate history comparisons with added/removed/changed/unchanged snapshot rows, transition-counter deltas, improved/regressed/mixed direction, independent audits, bounded diff queries, exact six-file diff-runtime persistence, CLI/API/schema/capability surfaces, public-surface registration, Actions coverage, and a real downloaded-ZIP diff demonstration.
65. Add policy-governed release review for remediation resolution history diffs. **Implemented:** configurable direction, readiness, count, transition-delta, and progression thresholds; explicit eligible/review/blocked and promote/hold/block decisions; ten addressed policy rules; independent 12-check policy audits; bounded rule queries with 10-check query audits; exact eight-file runtime persistence; fail-closed reloads, atomic writes, canonical addresses, CLI/API/schema/capability surfaces, public-surface registration, Actions coverage, and a real downloaded-ZIP policy demonstration.
66. Add a portable policy-review package for remediation resolution history diffs. **Implemented:** exact five-file handoff over the policy runtime, policy audit, runtime audit, derived summary, and manifest; canonical-byte and nested-link verification; independent 14-check package audits; bounded package summary, rule, audit, and runtime-check queries with 10-check query audits; deterministic JSON/CSV/Markdown projections; CLI/API/schema/capability surfaces; public-surface registration; Actions coverage; tamper regression coverage; and a real downloaded-ZIP package demonstration.
67. Add a policy-review package admission registry. **Implemented:** deterministic multi-package admission with duplicate identity rejection; empty/ready/review/blocked state folding; accepted, decision, and readiness conservation; exact four-file persistence; canonical-byte, manifest, summary, and public-boundary verification; independent 15-check registry audits; bounded summary/entry/readiness/decision queries with 10-check query audits; deterministic JSON/CSV/Markdown projections; CLI/API/schema/capability surfaces; public-surface registration; Actions coverage; tamper regression coverage; and a real downloaded-ZIP registry demonstration.
68. Add a cross-history policy package registry observatory. **Implemented:** deterministic member folding across distinct registry histories; latest state, decision, acceptance, readiness, and trend projections; flattened addressed transition retention with per-member snapshot conservation; exact five-file persistence; independent 16-check aggregate audits; bounded ten-resource summary/member/readiness/transition/trend queries with independent 10-check query audits; CLI/API/schema/capability surfaces; public-surface registration; Actions coverage; duplicate-identity, invalid-query, and tamper regression coverage; and a real downloaded-ZIP observatory demonstration.
69. Add portable ZIP transport for the policy package registry observatory. **Implemented:** deterministic six-member archive envelopes over the exact five-file observatory package; canonical JSON and byte-receipt replay; fixed ZIP ordering, timestamps, permissions, compression, and comments; atomic writes; strict duplicate/traversal/symlink/encryption/member-set rejection; independent 16-check archive audits with nested observatory verification; CLI/API/schema/capability surfaces; public-surface registration; Actions coverage; deterministic-byte, mapping, tamper, and malformed-archive regression coverage; and real downloaded-ZIP archive output.
70. Add bounded policy package registry observatory archive inspection. **Implemented:** manifest-only summary, manifest, artifact, file, nested-observatory, member, and transition resources; exact receipt, identity, state, decision, acceptance, readiness, transition, trend, and text filters; deterministic pagination and row addresses; independent 13-check query audits; CLI/API/schema/capability surfaces; public-surface registration; Actions coverage; fail-closed malformed-query coverage; and real downloaded-archive query demonstrations.
71. Add a persisted policy package registry observatory archive inspection runtime. **Implemented:** ordered load/verify/audit/query/query-audit/complete stage receipts; path-free runtime identity and acceptance folding; exact seven-file canonical persistence; per-file byte receipts and manifest-address replay; reload-time cross-link and tamper rejection; independent 15-check runtime audits; CLI/API/schema/capability surfaces; public-surface registration; Actions coverage; and real downloaded-archive runtime demonstrations.
72. Add bounded inspection queries over persisted archive runtimes. **Implemented:** summary, stage, lineage-link, component, and exact runtime-artifact resources; exact stage/state/acceptance/component/address/name/text filters; deterministic pagination and row addresses; public-runtime fallback that never invents unavailable artifact measurements; independent 15-check query audits; CLI/API/schema/capability surfaces; public-surface registration; Actions coverage; and real downloaded-archive runtime-query demonstrations.
73. Add durable filtered runtime-query snapshots. **Implemented:** exact five-file canonical handoffs containing the filtered query, independent query audit, snapshot summary, manifest, and per-file byte receipts; atomic persistence; strict reload and tamper rejection; runtime/query/audit linkage; independent 15-check snapshot audits; CLI/API/schema/capability surfaces; public-surface registration; Actions coverage; and real downloaded-archive snapshot demonstrations.
74. Add deterministic diffs between persisted runtime-query snapshots. **Implemented:** stable value-free row identities; added/removed/changed/unchanged classifications; changed-field evidence; conserved row and address deltas; improved/regressed/mixed direction; exact four-file diff persistence; independent 15-check diff audits; strict canonical reload and tamper rejection; CLI/API/schema/capability surfaces; public-surface registration; Actions coverage; and real downloaded-archive diff demonstrations.
75. Add bounded inspection queries over persisted runtime-query snapshot diffs. **Implemented:** summary, all-item, change-class, and changed-field resources; exact identity, source-resource, component, field, direction, state-transition, address, and text filters; deterministic pagination and row addresses; independent 12-check query audits; CLI/API/schema/capability surfaces; public-surface registration; Actions coverage; and real downloaded-archive diff-query demonstrations.
76. Add durable handoffs for persisted runtime-query snapshot diff queries. **Implemented:** exact five-file value-free snapshots containing source diff identity, filtered query pages, independent query audits, summaries, canonical manifests, and per-file byte receipts; atomic writes; strict reload and cross-link/tamper rejection; independent 15-check snapshot audits; CLI/API/schema/capability surfaces; public-surface registration; Actions coverage; and a real downloaded-archive handoff demonstration. See [docs/DOWNLOADED_DATA_DIFF_QUERY_SNAPSHOTS.md](DOWNLOADED_DATA_DIFF_QUERY_SNAPSHOTS.md).
77. Add longitudinal comparisons for persisted runtime-query snapshot diff queries. **Implemented:** same-shape query enforcement; stable `(resource, identity, field)` row pairing; added/removed/changed/unchanged classifications; endpoint source/query/audit receipt retention; changed-field evidence; improved/regressed/mixed/unchanged direction; exact four-file comparison persistence; independent 15-check comparison audits; strict canonical reload and tamper rejection; CLI/API/schema/capability surfaces; public-surface registration; Actions coverage; and persisted handoff comparison tests over downloaded data. See [docs/DOWNLOADED_DATA_QUERY_SNAPSHOT_COMPARISONS.md](DOWNLOADED_DATA_QUERY_SNAPSHOT_COMPARISONS.md).
78. Add bounded inspection queries over persisted query-snapshot comparisons. **Implemented:** summary, all-item, action-class, and changed-field projections; exact source-resource, stable-key, identity, field, direction, transition, address, and text filters; deterministic pagination and row addresses; independent 12-check query audits; CLI/API/schema/capability surfaces; public-surface registration; Actions coverage; and real downloaded-archive query demonstrations.
79. Add durable handoffs for persisted query-snapshot comparison queries. **Implemented:** exact five-file value-free snapshots containing comparison identity, complete query shape, filtered query rows, independent query audits, summaries, canonical manifests, and per-file byte receipts; atomic persistence; strict reload and cross-link/tamper rejection; independent 15-check snapshot audits; CLI/API/schema/capability surfaces; public-surface registration; Actions coverage; and a real downloaded-archive handoff demonstration.
80. Add a registry/observatory for persisted comparison-query snapshot handoffs. **Implemented:** deterministic multi-snapshot admission, duplicate identity rejection, state/acceptance/count folding, exact four-file persistence, strict canonical reload and byte receipts, independent 16-check registry audits, bounded eight-resource registry queries with independent 12-check query audits, CLI/API/schema/capability surfaces, public registration, Actions coverage, and a real downloaded-archive registry demonstration. See [docs/DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY.md](DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY.md).
81. Add longitudinal history for persisted comparison-query snapshot registries. **Implemented:** deterministic same-identity history admission, duplicate-address rejection, initial/improved/regressed/unchanged/changed transitions, exact four-file persistence, canonical reload and byte-safe replay, independent 16-check history audits, bounded history transition queries with independent 12-check query audits, CLI/API/schema/capability surfaces, public registration, Actions coverage, and real downloaded-archive history demonstration. See [docs/DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY.md](DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY.md).
82. Add a cross-history observatory for persisted comparison-query snapshot registry histories. **Implemented:** deterministic multi-history admission, member and transition folding, state/acceptance/trend summaries, exact five-file persistence, strict canonical reload and byte-safe replay, independent 15-check aggregate audits, bounded multi-resource queries with independent 11-check query audits, CLI/API/schema/capability surfaces, public registration, Actions coverage, and a real downloaded-archive observatory demonstration. See [docs/DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY_OBSERVATORY.md](DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY_OBSERVATORY.md).
83. Add a transportable archive for the cross-history observatory. **Implemented:** deterministic fixed-metadata ZIP transport over the exact five-file observatory package; canonical manifest and per-artifact byte receipts; independent 17-check archive audits; fail-closed member-set, traversal, canonical-byte, and replay verification; bounded archive and query-audit surfaces; CLI/API/schema/capability surfaces; public registration; Actions coverage; and a real downloaded-archive transport demonstration. See [docs/DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY_OBSERVATORY_ARCHIVE.md](DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY_OBSERVATORY_ARCHIVE.md).
84. Add resumable transport for the cross-history observatory archive. **Implemented:** deterministic addressed byte-range chunking over the verified ZIP; complete and partial canonical transfer directories; out-of-order idempotent resume; progress receipts and missing-chunk inspection; fail-closed chunk, manifest, and nested-archive verification; independent 16-check transfer audits and 12-check transfer-query audits; bounded manifest, chunk, received, missing, and progress queries; CLI/API/schema/capability surfaces; public registration; Actions coverage; negative controls; and a real downloaded-archive transfer demonstration. See [DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY_OBSERVATORY_ARCHIVE_TRANSFER.md](DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY_OBSERVATORY_ARCHIVE_TRANSFER.md).
85. Add path-free recovery planning for partial observatory archive transfers. **Implemented:** deterministic complete/partial recovery snapshots; addressed actions for every missing chunk; explicit resume-versus-assemble decisions; checkpoint safety and next-index projections; independent 15-check recovery audits; bounded summary/action/received/missing/state/bounds queries with independent 12-check query audits; CLI/API/schema/capability surfaces; public registration; Actions coverage; tamper and round-trip regression coverage; and a real downloaded-archive checkpoint demonstration. See [DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY_OBSERVATORY_ARCHIVE_TRANSFER_RECOVERY.md](DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY_OBSERVATORY_ARCHIVE_TRANSFER_RECOVERY.md).
86. Add verifiable recovery execution receipts for partial observatory archive transfers. **Implemented:** deterministic planned/in-progress/complete/blocked states; assembler-backed applied-chunk verification; explicit pending/rejected outcomes; byte/index conservation; independent 18-check execution audits; bounded seven-resource execution queries with independent 12-check query audits; CLI/API/schema/capability surfaces; public registration; tamper and round-trip coverage; and a real downloaded-archive progression demonstration. See [DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY_OBSERVATORY_ARCHIVE_TRANSFER_RECOVERY_EXECUTION.md](DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY_OBSERVATORY_ARCHIVE_TRANSFER_RECOVERY_EXECUTION.md).
87. Add a durable runtime handoff for recovery execution receipts. **Implemented:** atomic seven-file runtime packages; canonical manifests and byte receipts; exact reload and fail-closed verification; fixed five-stage readiness state; independent 16-check runtime audits; bounded seven-resource runtime queries with independent 12-check query audits; CLI/API/schema/capability surfaces; public registration; Actions coverage; and a real downloaded-archive runtime demonstration. See [DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY_OBSERVATORY_ARCHIVE_TRANSFER_RECOVERY_EXECUTION_RUNTIME.md](DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY_OBSERVATORY_ARCHIVE_TRANSFER_RECOVERY_EXECUTION_RUNTIME.md).
88. Add a registry for recovery execution runtime handoffs. **Implemented:** deterministic multi-runtime admission with duplicate identity rejection; empty/ready/blocked state folding; accepted/ready/blocked conservation; exact four-file atomic persistence; canonical reload and tamper rejection; independent 16-check registry audits; bounded seven-resource registry queries with independent 12-check query audits; CLI/API/schema/capability surfaces; public registration; Actions coverage; and a real downloaded-archive registry demonstration. See [DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY_OBSERVATORY_ARCHIVE_TRANSFER_RECOVERY_EXECUTION_RUNTIME_REGISTRY.md](DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY_OBSERVATORY_ARCHIVE_TRANSFER_RECOVERY_EXECUTION_RUNTIME_REGISTRY.md).
89. Add federation over recovery execution runtime registries. **Implemented:** deterministic source-scoped multi-registry admission with duplicate identity rejection; flattened runtime-entry provenance; empty/ready/blocked/mixed state folding; member and runtime count conservation; exact five-file atomic persistence; canonical reload and tamper rejection; independent 18-check federation audits; bounded ten-resource federation queries with independent 12-check query audits; CLI/API/schema/capability surfaces; public registration; Actions coverage; and a real downloaded-archive federation demonstration. See [DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY_OBSERVATORY_ARCHIVE_TRANSFER_RECOVERY_EXECUTION_RUNTIME_REGISTRY_FEDERATION.md](DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY_OBSERVATORY_ARCHIVE_TRANSFER_RECOVERY_EXECUTION_RUNTIME_REGISTRY_FEDERATION.md).
90. Add a portable deterministic archive over runtime-registry federations. **Implemented:** fixed six-member ZIP packaging over the exact five-file federation; canonical outer manifest and per-member byte receipts; deterministic metadata and byte-size conservation; atomic file persistence; fail-closed member-order, traversal, symlink, encryption, comment, canonical-byte, nested-projection, and tamper rejection; independent 18-check archive audits; bounded ten-resource archive queries with independent 12-check query audits; CLI/API/schema/capability surfaces; public registration; Actions coverage; and a real downloaded-archive federation-package demonstration. See [DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY_OBSERVATORY_ARCHIVE_TRANSFER_RECOVERY_EXECUTION_RUNTIME_REGISTRY_FEDERATION_ARCHIVE.md](DOWNLOADED_DATA_COMPARISON_QUERY_SNAPSHOT_REGISTRY_HISTORY_OBSERVATORY_ARCHIVE_TRANSFER_RECOVERY_EXECUTION_RUNTIME_REGISTRY_FEDERATION_ARCHIVE.md).

Every wave should preserve the same contracts, add fixtures and failure cases, and distinguish implemented behavior from externally evaluated science.

## Change acceptance checklist

- input and output versions are recorded;
- unsupported paths abstain explicitly;
- negative controls are included where the task permits;
- privacy and license checks are documented;
- deterministic reruns produce the same addresses; and
- review-facing explanations retain edge-level provenance.

No roadmap item is a claim of completion until code, fixtures, tests, and review material exist together.

The public repository should remain useful with no private dataset installed.

Build waves should be reversible.
Persisted artifacts should be readable.
Benchmarks should include failures.
Adapters should declare limitations.
Review should remain accountable.
