# Workspace beta frontier build record

## C05-C08 build

- topology viewport is covered by exact-context, foreign-context, invalid-focus,
  and empty-observation cases;
- causal chain is covered by complete, foreign, incomplete, contradictory, and
  alternative-path cases;
- posterior decomposition is covered by reconciled, foreign-component,
  unreconciled, and missing-support cases;
- evidence table is covered by filtered, foreign-context, empty-result, and
  paginated cases;
- public aggregate source receipts, contract schemas, policy decisions,
  lineage, replay, quality, runtime, observability, artifacts, review queue,
  and CSV export are included;
- serialized projection assertions, accessibility, boundary compliance, and
  the 128-case validation matrix provide post-execution checks;
- the capability registry promotes C05-C08 to verified after focused and full
  test evidence passes.

The package is research infrastructure. It retains uncertainty and unresolved
states and does not turn a projection into a clinical or operational decision.

The build also keeps reproducible package addresses, deterministic runbook
steps, bounded thresholds, stable output ordering, and explicit source scope.

All controls remain replayable.
